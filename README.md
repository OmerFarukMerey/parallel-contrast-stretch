# parallel-contrast-stretch
CUDA-accelerated automatic contrast enhancement for grayscale BMPs, parallel min/max reduction in shared memory, plus per-pixel subtract and scale kernels, benchmarked against a CPU baseline.
